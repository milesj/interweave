{
  "name": "interweave-root",
  "private": true,
  "scripts": {
    "build": "beemo typescript --workspaces=* --priority --react",
    "coverage": "yarn run jest --coverage",
    "eslint": "beemo eslint",
    "jest": "beemo jest --react",
    "posttest": "yarn run eslint",
    "pretest": "yarn run type",
    "test": "yarn run jest",
    "test:live": "yarn run jest && webpack && serve ./tests/",
    "prettier": "beemo prettier",
    "prerelease": "yarn test && yarn run build",
    "release": "lerna publish",
    "type": "beemo typescript --workspaces=* --noEmit --react"
  },
  "devDependencies": {
    "@milesj/build-tools": "^0.1.2",
    "@types/react": "^16.4.14",
    "@types/react-dom": "^16.0.7",
    "babel-loader": "^8.0.2",
    "emojibase": "^2.0.0",
    "emojibase-test-utils": "^3.0.0",
    "lerna": "^3.4.0",
    "react": "^16.5.2",
    "serve": "^10.0.2",
    "ts-loader": "^5.2.0",
    "webpack": "^4.19.1",
    "webpack-cli": "^3.1.1"
  },
  "workspaces": [
    "packages/*"
  ],
  "beemo": {
    "module": "@milesj/build-tools",
    "drivers": [
      "babel",
      "eslint",
      "jest",
      "prettier",
      "typescript"
    ],
    "eslint": {
      "rules": {
        "import/no-named-as-default": "off",
        "react/jsx-no-literals": "off"
      }
    }
  },
  "browserslist": [
    "ie 10"
  ]
}
